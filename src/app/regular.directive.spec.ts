import { RegularDirective } from './regular.directive';

describe('RegularDirective', () => {
  it('should create an instance', () => {
    const directive = new RegularDirective();
    expect(directive).toBeTruthy();
  });
});
